[package]
name = "vapor-forge-tool-support"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Argument and hex-dump parsing shared by the developer tool binaries. Kept
# dependency-free so `game-bridge` can use it without giving up its "buildable
# for any target, no external dependencies" property.
[dependencies]

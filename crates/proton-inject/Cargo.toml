[package]
name = "proton-inject"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[lib]
crate-type = ["cdylib"]

[dependencies]
inject-protocol = { path = "../inject-protocol" }
libc = "0.2"

[package]
name = "vapor-forge-inject-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
autobins = false

[features]
default = []
tools = []

[[bin]]
name = "vapor-forge-ipc-tool"
path = "src/bin/vapor-forge-ipc-tool.rs"
required-features = ["tools"]

# No external dependencies. Pure Rust, buildable for any target (i686 / x86_64).
[dependencies]

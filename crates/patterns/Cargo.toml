[package]
name = "vapor-forge-patterns"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
thiserror.workspace = true

[build-dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

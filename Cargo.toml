[workspace]
members = [
    "crates/audit-loader",
    "crates/diagnostics",
    "crates/diagnostics-cli",
    "crates/hook-boundary",
    "crates/hooks",
    "crates/memory",
    "crates/patterns",
    "crates/config",
    "crates/runtime-core",
    "crates/steam-abi",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "AGPL-3.0-only"
repository = ""
rust-version = "1.80"

[workspace.dependencies]
iced-x86 = { version = "1.21", default-features = false, features = ["std", "decoder", "encoder", "instr_info"] }
libc = "0.2"
object = { version = "0.39.1", default-features = false, features = ["read"] }
once_cell = "1"
parking_lot = "0.12"
retour = "0.4.0-alpha.4"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"
tracing = "0.1"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = 3
